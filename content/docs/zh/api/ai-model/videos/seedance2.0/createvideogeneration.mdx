---
title: 创建视频生成任务
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: |
          通过 `POST /v1/video/generations` 调用 Seedance 2.0（`seedance-2-0` / `seedance-2-0-fast`），支持文生视频、图生视频与多模态 `content` 数组。

          `asset://` 须按素材类型映射到 `image_url` / `video_url` / `audio_url`；提供 `reference_audio` 时建议 `generate_audio: false`。

          详细说明见仓库内《Seedance 2.0.md》用户文档。
---

通过 `POST /v1/video/generations` 调用 Seedance 2.0（`seedance-2-0` / `seedance-2-0-fast`），支持文生视频、图生视频与多模态 `content` 数组。请求体字段与《Seedance 2.0.md》「创建视频生成任务」章节一致（顶层 `content`、`duration`、`resolution`、`ratio` 等）。

<APIPage document={"openapi/generated/ai-model/视频（Videos）/Seedance2.0格式/post-v1-video-generations-createvideogeneration-383844576.json"} operations={[{"path":"/v1/video/generations","method":"post"}]} />
